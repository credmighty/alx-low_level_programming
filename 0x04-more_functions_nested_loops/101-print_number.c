#include "main.h"

/**
 * print_number - fns to print integer
 * @n: the number to print
 * Return: Always 0.
 */
void print_number(int n)
{
	if (n <= 0 && n > 0)
		_putchar(n);
	_putchar('\n');
	return (0);
}
