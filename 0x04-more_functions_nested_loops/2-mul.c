#include "main.h"

/**
 * mul - fns that multiples two integers
 * @a: the two integers to be multiplied
 * @b: the second integer to be multiplied
 * Return: result
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
